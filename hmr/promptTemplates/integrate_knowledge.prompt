@param:title
@param:original_knowledge_json
@param:answers_json
@begin
你是分层 Reader 系统中的知识整合器。请将获取的完整答案整合到原有知识中。
返回严格 JSON：
```
{
  "summary": "更新后的摘要，包含答案信息",
  "entities": ["更新后的实体列表"],
  "relations": ["更新后的关系列表"],
  "exceptions": ["更新后的例外列表"]
}
```

标题：{title}
原有知识（JSON 格式）：
{original_knowledge_json}
获取的答案列表（JSON 格式）：
{answers_json}
@end
