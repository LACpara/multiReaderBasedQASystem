[project]
name = "hmr-demo"
version = "0.1.0"
description = "Demo for a Hierarchical Multi-Agent Reader Retrieval System"
requires-python = ">=3.10"
dependencies = ["chromadb>=0.5.0"]

[tool.ruff]
line-length = 100
